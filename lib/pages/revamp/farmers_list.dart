import 'package:flutter/material.dart';

class FarmersListPage extends StatelessWidget {
  const FarmersListPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text("Farmers List")),
    );
  }
}
